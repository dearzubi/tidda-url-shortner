# COMMITTED FILE. No secrets here. Values are visible to anyone with repo
# access. Safe defaults shared across modes. Mode-specific defaults live in
# `.env.[mode]`. Put sensitive or machine-specific overrides in
# `.env.local` (gitignored).
