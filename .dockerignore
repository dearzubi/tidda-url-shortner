# Keep Docker build contexts small and prevent local-only files from reaching
# the Docker daemon. Build stages explicitly copy the files they need.

.git
.github
.cache
.claude
.codex
.DS_Store

node_modules
**/node_modules
.pnpm-store

dist
build
coverage
.vite
*.tsbuildinfo

# Never send local secrets or machine-specific overrides to Docker.
/.env
.env.local
.env.*.local
**/.env.local
**/.env.*.local

*.log
npm-debug.log*
pnpm-debug.log*

# Terraform local state and provider caches are never needed in app images.
**/.terraform
*.tfstate
*.tfstate.*
*.tfplan
*.tfvars
!*.tfvars.example
